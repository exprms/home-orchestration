# from dagster import EnvVar
# import sqlite3

# con = sqlite3.connect(DATABASE_FILEPATH + DATABASE_NAME)

# learning_app_db = DuckDBResource(
#     database=EnvVar("DUCKDB_DATABASE")      # replaced with environment variable
# )